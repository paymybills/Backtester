services:
  - type: web
    name: backtesting-engine-api
    env: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn ui.backend:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
